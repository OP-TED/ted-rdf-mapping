#title: I.1.4 - Town
#description: “I.1.4 - Town” in SF corresponds to “BT-513 Organisation City” in eForms. The corresponding XML element is nanADDRESS_CONTRACTING_BODY/TOWN. The expected ontology instances are epo: epo:Role (from CL1) / epo:Organisation (from CL2) /  locn:Address / rdf:langString .

PREFIX epo: <http://data.europa.eu/a4g/ontology#>
PREFIX locn: <http://www.w3.org/ns/locn#>

ASK WHERE { ?this epo:playedBy / epo:hasRegisteredAddress / locn:postName ?value . }